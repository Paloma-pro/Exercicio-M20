
function qtdd(arr) {
    return arr.length;
}

function primeiroElem(arr) {
    return arr[0];
}

function ultimoElem(arr) {
    return arr[arr.length - 1];
}


module.exports = { 
    qtdd, 
    primeiroElem, 
    ultimoElem, 
}